{
    "functions": {
        "api/**/*.ts": {
            "memory": 1024,
            "maxDuration": 60
        }
    },
    "rewrites": [
        {
            "source": "/api/:path((?!test).*)",
            "destination": "/api"
        }
    ]
}